# Equity Daily AI Signals Generation
# Uses v3.0 Constrained Autonomy with quant setup integration
# Runs after equity features calculation to generate AI trading signals

name: Equity Daily AI Signals

on:
  # Run Mon-Fri at 22:30 UTC (5:30 PM ET, 1h after features)
  schedule:
    - cron: '30 22 * * 1-5'
  
  # Allow manual trigger
  workflow_dispatch:
    inputs:
      target_date:
        description: 'Target date (YYYY-MM-DD). Leave empty for yesterday.'
        required: false
        type: string
      model:
        description: 'Gemini model to use'
        required: false
        default: 'gemini-3-flash-preview'
        type: string
      limit:
        description: 'Number of top equities to process'
        required: false
        default: '500'
        type: string

jobs:
  generate-signals:
    name: Generate Equity AI Signals
    runs-on: ubuntu-latest

    timeout-minutes: 180
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
          pip install google-genai
      
      - name: Determine target date
        id: date
        run: |
          if [ -n "${{ github.event.inputs.target_date }}" ]; then
            echo "date=${{ github.event.inputs.target_date }}" >> $GITHUB_OUTPUT
          else
            # Default to yesterday (UTC)
            echo "date=$(date -u -d 'yesterday' +%Y-%m-%d)" >> $GITHUB_OUTPUT
          fi
      
      - name: Determine model
        id: model
        run: |
          if [ -n "${{ github.event.inputs.model }}" ]; then
            echo "model=${{ github.event.inputs.model }}" >> $GITHUB_OUTPUT
          else
            echo "model=gemini-3-flash-preview" >> $GITHUB_OUTPUT
          fi
      
      - name: Determine limit
        id: limit
        run: |
          if [ -n "${{ github.event.inputs.limit }}" ]; then
            echo "limit=${{ github.event.inputs.limit }}" >> $GITHUB_OUTPUT
          else
            echo "limit=500" >> $GITHUB_OUTPUT
          fi
      
      - name: Run equity AI signals generation (v3.0 Constrained Autonomy)
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          python scripts/run_ai_analysis_v3.py --date ${{ steps.date.outputs.date }} --model ${{ steps.model.outputs.model }} --limit ${{ steps.limit.outputs.limit }} --asset-type equity
